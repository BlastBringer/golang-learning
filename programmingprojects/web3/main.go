package main

import (
	"bufio"
	"fmt"
	"os"
)


func main(){
	fmt.Println("Hello world!")
	arr := make([]int, 10)
	for {
		val := 0
		fmt.Println("Enter the array elements: ")
		scanner := bufio.NewScanner(os.Stdin)
		scanner.
		arr = append(arr, val)
	}
	fmt.Println(arr)
}